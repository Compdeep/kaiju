{"query": "add a second parameter `bool doubleIt = false` to compute(int x). When doubleIt is true, return x * 2. When false (default), return x. Both signatures must compile — existing callers using `compute(x)` must still work.", "check": "tmp=$(mktemp -d) && cp $FILE $tmp/compute.cpp && g++ -std=c++17 -o $tmp/b $tmp/compute.cpp tests/eval/editor/corpus/cpp/compute_driver.cpp && $tmp/b"}
