{"core": true, "query": "change the global scrape_interval from 15s to 60s, leaving the per-job scrape_interval on the app job at 30s", "check": "python3 -c \"import yaml; d=yaml.safe_load(open('$FILE')); assert d['global']['scrape_interval']=='60s'; jobs={j['job_name']:j for j in d['scrape_configs']}; assert jobs['app']['scrape_interval']=='30s'\""}
{"query": "add a third job named redis scraping localhost:9121, keeping the node and app jobs unchanged", "check": "python3 -c \"import yaml; d=yaml.safe_load(open('$FILE')); names=[j['job_name'] for j in d['scrape_configs']]; assert 'redis' in names, names; assert 'node' in names and 'app' in names\""}
