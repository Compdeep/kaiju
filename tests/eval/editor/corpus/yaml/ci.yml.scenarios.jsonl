{"core": true, "query": "bump the go-version from 1.22 to 1.23 and raise timeout-minutes from 10 to 20", "check": "python3 -c \"import yaml; d=yaml.safe_load(open('$FILE')); j=d['jobs']['test']; assert j['timeout-minutes']==20, j; assert str(j['steps'][1]['with']['go-version'])=='1.23'\""}
{"query": "add a step named Vet that runs 'go vet ./...' after the Build step, keeping Build and Test unchanged", "check": "python3 -c \"import yaml; d=yaml.safe_load(open('$FILE')); s=d['jobs']['test']['steps']; names=[x.get('name') for x in s]; assert 'Vet' in names, names; runs=[x.get('run') for x in s]; assert 'go vet ./...' in runs; assert 'go build ./...' in runs; assert 'go test ./...' in runs\""}
