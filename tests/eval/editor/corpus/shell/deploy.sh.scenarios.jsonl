{"core": true, "query": "keep 10 releases instead of 5 and raise the health check timeout from 30 to 120 seconds, changing nothing else", "check": "bash -n $FILE && grep -q 'RELEASES_KEPT=10' $FILE && grep -q 'TIMEOUT=120' $FILE && grep -q 'systemctl restart app.service' $FILE && grep -q 'set -euo pipefail' $FILE"}
{"query": "change the health URL port from 3000 to 8080, leaving the path /health alone", "check": "bash -n $FILE && grep -q '127.0.0.1:8080/health' $FILE && ! grep -q ':3000' $FILE"}
{"query": "add a function named rollback that logs 'rolling back' and returns 1, placed before main, keeping every existing function", "check": "bash -n $FILE && grep -q 'rollback()' $FILE && grep -q 'rolling back' $FILE && grep -q 'prune_releases()' $FILE && grep -q 'wait_for_health()' $FILE"}
