{"query": "append a line `target_compile_options(sampleapp PRIVATE -Wall -Wextra -Wpedantic)` AFTER the existing target_include_directories call. Do not modify earlier lines.", "check": "grep -qE 'target_compile_options\\s*\\(\\s*sampleapp\\s+PRIVATE\\s+-Wall\\s+-Wextra\\s+-Wpedantic\\s*\\)' $FILE && awk '/target_include_directories/{inc=NR} /target_compile_options/{opt=NR} END{exit !(opt > inc)}' $FILE"}
{"query": "add `find_package(Threads REQUIRED)` near the top (after project()), and append `target_link_libraries(sampleapp PRIVATE Threads::Threads)` at the bottom.", "check": "grep -qE 'find_package\\s*\\(\\s*Threads\\s+REQUIRED\\s*\\)' $FILE && grep -qE 'target_link_libraries\\s*\\(\\s*sampleapp\\s+PRIVATE\\s+Threads::Threads\\s*\\)' $FILE"}
