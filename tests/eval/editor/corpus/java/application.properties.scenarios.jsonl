{"core": true, "query": "change server.port from 8080 to 9090 and set spring.jpa.show-sql to true, leaving every other property alone", "check": "grep -q '^server.port=9090' $FILE && grep -q '^spring.jpa.show-sql=true' $FILE && grep -q '^server.servlet.context-path=/api' $FILE && grep -q '^spring.jpa.hibernate.ddl-auto=validate' $FILE"}
{"query": "change logging.level.root from INFO to WARN and leave logging.level.com.example at DEBUG", "check": "grep -q '^logging.level.root=WARN' $FILE && grep -q '^logging.level.com.example=DEBUG' $FILE && ! grep -q '^logging.level.root=INFO' $FILE"}
{"query": "add metrics and prometheus to management.endpoints.web.exposure.include, keeping health and info", "check": "grep -q '^management.endpoints.web.exposure.include=' $FILE && grep 'exposure.include' $FILE | grep -q health && grep 'exposure.include' $FILE | grep -q info && grep 'exposure.include' $FILE | grep -q metrics && grep 'exposure.include' $FILE | grep -q prometheus"}
