{"core": true, "query": "change the listening port from 80 to 8080, leaving ServerName, DocumentRoot and every other directive exactly as they are", "check": "grep -q '<VirtualHost \\*:8080>' $FILE && grep -q 'ServerName example.internal' $FILE && grep -q 'DocumentRoot /var/www/example' $FILE && ! grep -q '<VirtualHost \\*:80>' $FILE"}
{"query": "raise KeepAliveTimeout from 5 to 15 and leave Timeout at 60", "check": "grep -q 'KeepAliveTimeout 15' $FILE && grep -q 'Timeout 60' $FILE && ! grep -q 'KeepAliveTimeout 5' $FILE"}
{"query": "add a Header directive setting X-Frame-Options to SAMEORIGIN inside the VirtualHost, keeping every existing directive", "check": "grep -qi 'X-Frame-Options' $FILE && grep -q 'SAMEORIGIN' $FILE && grep -q 'DocumentRoot /var/www/example' $FILE && grep -q 'KeepAlive On' $FILE"}
{"query": "change AllowOverride from None to All inside the Directory block, changing nothing else", "check": "grep -q 'AllowOverride All' $FILE && ! grep -q 'AllowOverride None' $FILE && grep -q 'Require all granted' $FILE && grep -q 'Options -Indexes +FollowSymLinks' $FILE"}
