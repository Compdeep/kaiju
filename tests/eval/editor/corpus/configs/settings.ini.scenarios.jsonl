{"query": "raise workers from 4 to 16 and pool_size from 10 to 40, leaving the timeout at 30", "check": "python3 -c \"import configparser; c=configparser.ConfigParser(); c.read('$FILE'); assert c['server']['workers']=='16'; assert c['database']['pool_size']=='40'; assert c['database']['timeout']=='30'\""}
{"query": "change the logging level from info to warning, changing nothing else", "check": "python3 -c \"import configparser; c=configparser.ConfigParser(); c.read('$FILE'); assert c['logging']['level']=='warning'; assert c['logging']['format']=='json'; assert c['server']['port']=='8000'\""}
