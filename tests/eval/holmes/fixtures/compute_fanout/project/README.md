# compute_fanout fixture

Empty project. The test query drives a value-computation
pipeline (fetch-compute-rank) to exercise ${step.N.field}
template substitution, JSON truncation, and data-flow checks
end-to-end.
